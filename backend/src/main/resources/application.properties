spring.application.name=Barberini
server.port=${PORT:8080}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

app.jwt.secret=${APP_JWT_SECRET:barberini-segredo-dev-mude-em-producao-1234567890}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:7776000000}
app.google.client-id=${GOOGLE_CLIENT_ID:868389533637-d3l4a0mrnnbf7i1h34cd0mts996sb6pc.apps.googleusercontent.com}

# Dono padrão (criado no seed se não existir)
app.seed.dono.email=dono@barberini.com
app.seed.dono.senha=dono123
app.seed.dono.nome=Dono Barberini
